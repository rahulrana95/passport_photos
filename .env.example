# OPTIONAL on Vercel — the production URL is derived automatically.
#
# Set this only for a custom domain, or when deploying somewhere that does not
# provide VERCEL_PROJECT_PRODUCTION_URL. Absolute origin; a trailing slash is
# stripped for you. Used for canonical URLs, Open Graph tags and the sitemap.
NEXT_PUBLIC_SITE_URL=https://example.com
